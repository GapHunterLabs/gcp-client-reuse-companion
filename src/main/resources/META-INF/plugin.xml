<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.gcpclientreusecompanion</id>
    <name>GCP Client Reuse Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Gutter warning icon on a Google Cloud client library
        (<code>StorageOptions</code>, <code>FirestoreOptions</code>,
        <code>BigQueryOptions</code>, <code>PubSubOptions</code>, and
        similar) built via
        <code>XxxOptions.getDefaultInstance().getService()</code> inside
        a regular method body -- Google Cloud's own client libraries
        best-practices documentation says: "you should reuse the same
        client object for many requests when possible, instead of
        creating a new one for every request", explaining that each
        client instance has its own credential cache and that "creating
        too many in a small period of time may incur rate limiting
        causing library requests to fail authentication".</p>
        <p><b>100% static text/PSI analysis</b> -- matches the options
        class name by simple text, so it works whether the real Google
        Cloud jar is on the classpath or not. Java and Kotlin.</p>
        <p>Stated honestly: v0.1 only flags the direct
        <code>.getDefaultInstance().getService()</code> chain -- an
        options instance assigned to an intermediate variable before
        <code>.getService()</code> is called isn't specially traced.
        Never flags a call inside a constructor or a field/property
        initializer (legitimate "create once" locations).</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK1="true" supportsK2="true"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.gcpclientreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <codeInsight.lineMarkerProvider language="kotlin"
            implementationClass="dev.gaphunter.gcpclientreusecompanion.gutter.ClientBuiltPerCallLineMarkerProvider"/>
        <notificationGroup id="GCP Client Reuse Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
